# Dependencies
node_modules/

# Build output
dist/

# Logs
*.log

# OS / editor junk
Thumbs.db
desktop.ini
.DS_Store

# Claude Code working files (kept local, not published)
.claude/

# Internal context notes for AI assistants (kept local, not published)
CLAUDE.md
AGENTS.md

# Local dev launcher (kept local, not published)
Lumina-DEV.bat
Lumina-DIAG.bat

# Local roadmap snapshot (kept local, not published)
ROADMAP.md

# Local handoff log between sessions/agents (kept local, not published)
STATUS.md

# Local task/plan docs handed to the dev agent (kept local, not published)
plans/

# Bundled Wallhaven API key (official builds only) — NEVER publish.
# Create wallhaven-key.json with {"apikey":"..."} for NSFW in official builds.
wallhaven-key.json

# Bundled Gelbooru credentials (official builds only) — NEVER publish.
# Create gelbooru-key.json with {"userId":"...","apiKey":"..."}.
gelbooru-key.json
